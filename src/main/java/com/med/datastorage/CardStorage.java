package com.med.datastorage;

import org.springframework.stereotype.Component;

/**
 * Created by george on 13.05.18.
 */
@Component
public class CardStorage {

 /*   @Autowired
    ProcedureServiceImpl procedureService;

    @Autowired
    CardServiceImpl service;


    @PostConstruct
    void init(){

        list = new ArrayList<>();

        registration = procedureService.getProcedure(1);
        registrationCard.setId(registration.getId());
        registrationCard.setName(registration.getName());
        registrationCard.setDays(1);
        cards.add(registrationCard);

        diagnostics = procedureService.getProcedure(2);
        diagnosticsCard.setId(diagnostics.getId());
        diagnosticsCard.setName(diagnostics.getName());
        diagnosticsCard.setDays(1);
        cards.add(diagnosticsCard);

        manual = procedureService.getProcedure(3);
        manualCard.setId(manual.getId());
        manualCard.setName(manual.getName());
        manualCard.setDays(1);
        cards.add(manualCard);

        pulling = procedureService.getProcedure(4);
        pullingCard.setId(pulling.getId());
        pullingCard.setName(pulling.getName());
        pullingCard.setDays(1);
        //////////////////
        cards.add(pullingCard);








    }

    private List<Procedure> list;

    private Procedure registration;
    private Procedure manual;
    private Procedure diagnostics;
    private Procedure pulling;









    private Card registrationCard;
    private Card manualCard;
    private Card diagnosticsCard;
    private Card pullingCard;




   private List<Card> cards = new ArrayList<>(
            Arrays.asList(







            )
    );




*/
}
